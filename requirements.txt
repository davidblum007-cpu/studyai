# ── Core Backend ──────────────────────────────────────────────────────────────
anthropic>=0.50.0
flask>=3.1.0
flask-cors>=5.0.0
flask-limiter>=3.0.0
python-dotenv>=1.0.1
werkzeug>=3.1.3

# ── PDF-Verarbeitung ──────────────────────────────────────────────────────────
PyPDF2>=3.0.1
pdfplumber>=0.11.4

# ── Flashcard-Export ──────────────────────────────────────────────────────────
genanki>=0.13.0

# ── ML / Spaced Repetition ───────────────────────────────────────────────────
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0

# ── Web-Suche (Flashcard-Kontext) ─────────────────────────────────────────────
duckduckgo-search>=6.0.0

# ── Firebase Firestore Cloud-Sync (optional) ──────────────────────────────────
# Setup: python firebase_setup.py <credentials.json>
firebase-admin>=6.0.0

# ── Stripe Billing (Phase 2, optional bis Monetarisierung aktiv) ──────────────
# Benötigt für: /api/billing/checkout, /api/billing/portal, /api/webhooks/stripe
stripe>=8.0.0

# ── Produktions-Server (Phase 3) ──────────────────────────────────────────────
# gunicorn.conf.py nutzt gthread-Worker-Class für SSE-Streaming-Kompatibilität.
# Nur für Linux/macOS nötig – auf Windows: python server.py für Entwicklung.
gunicorn>=22.0.0

# ── Redis (Phase 3 – Rate-Limiting + optionales Caching) ─────────────────────
redis>=5.0.0

# ── Sentry Error-Tracking (Phase 3, optional) ─────────────────────────────────
# Aktivieren: SENTRY_DSN=https://xxx@sentry.io/yyy in .env setzen
sentry-sdk[flask]>=2.0.0
